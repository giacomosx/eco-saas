import React from 'react';
import MainLayout from "../MainLayout";

const HomePage = () => {
    return (
        <MainLayout>
            Homepage
        </MainLayout>
    );
};

export default HomePage;
