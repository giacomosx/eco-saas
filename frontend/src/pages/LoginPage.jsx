import React from 'react';
import MainLayout from "../MainLayout";

const LoginPage = () => {
    return (
        <MainLayout>
            Login
        </MainLayout>
    );
};

export default LoginPage;
